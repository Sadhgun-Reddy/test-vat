# Environment Variables Template
# Copy this file to .env, .env.development.local, etc. and update the values.
# DO NOT commit actual secrets (API keys, passwords, etc.) to version control.

# ==========================================
# API Configuration
# ==========================================
# The base URL for the backend API.
# In development, this is typically http://localhost:4000/api/v1
# In production, set this to your deployed API origin (e.g., https://api.yourdomain.com/api/v1)
VITE_API_URL=http://193.203.162.8/api/v1
# VITE_API_URL=http://localhost:4000/api/v1

# Add other necessary environment variables below:
# e.g., VITE_FEATURE_FLAG_X=true
