# Backend API (must match CORS on server). Restart `npm start` after changes.
VITE_API_URL=http://193.203.162.8/api/v1
# Set to true only for offline demo cards (no real JWT). Default: real login only.
VITE_DEMO_LOGIN=false
